# مراجعة شاملة لاستعادة أحداث `IN_PROGRESS`

## النطاق

تمت مراجعة دورة الحدث في `SecurityPrefs`، وتشغيل الخدمة، و`BootReceiver`، وتهيئة `SecurityViewModel`، مع التركيز على ما يحدث عند:

- إغلاق التطبيق أو قتل العملية.
- إعادة تشغيل الجهاز.
- توقف الخدمة أثناء الالتقاط أو الإرسال.
- نجاح فتح القفل أثناء وجود حدث قيد التنفيذ.
- وصول Intent مكرر أو متأخر.

## الحكم العام

التصميم الحالي أفضل من تخزين عداد فقط: لكل حدث `eventId` وحالة ووقت تحديث وعدد محاولات استعادة. توجد حماية جيدة من بعض التكرارات، لكن الاستعادة **ليست مكتملة تشغيليًا**؛ إذ يتم تحويل الأحداث القديمة إلى `PENDING` أحيانًا، دون وجود موزّع دائم يضمن إعادة تشغيلها، كما أن الحدث الذي يتوقف ثم يعاد تمريره قبل انتهاء مهلة الحجز قد يُرفض ولا يُعاد جدولتُه.

النتيجة العملية: النظام يمنع بعض الإرسالات المكررة، لكنه قد يترك حدثًا مستعادًا في الطابور دون تنفيذ، أو يفقد فرصة إعادة معالجته إذا وصل Intent أثناء وجوده في `IN_PROGRESS` حديثة.

## دورة الحدث الحالية

```text
onPasswordFailed
  -> registerFailedUnlockAttempt
  -> إذا بلغ الحد: enqueueSecurityEvent
  -> event = PENDING
  -> startForegroundService مع eventId
  -> onCreate: recoverStaleSecurityEvents
  -> onStartCommand
  -> claimSecurityEvent(eventId)
  -> IN_PROGRESS
  -> capture + location + SMTP
  -> SENT أو FAILED
```

وعند `onPasswordSucceeded`:

```text
reset counter
cancel PENDING و IN_PROGRESS
cancel countdown
```

## ما يعمل بشكل جيد

### 1. هوية فريدة للحدث

إنشاء `UUID` لكل حدث يمنع الاعتماد على الوقت وحده، ويسمح بمنع معالجة نفس Intent مرتين.

### 2. المطالبة الذرية

`claimSecurityEvent` تستخدم `@Synchronized` وتقبل الحدث فقط عندما تكون حالته `PENDING`. هذا يمنع عاملين داخل العملية نفسها من معالجة الحدث نفسه في الوقت ذاته.

### 3. إلغاء الأحداث عند فتح القفل

تحويل `PENDING` و`IN_PROGRESS` إلى `CANCELLED` يمنع الأحداث القديمة من الاستمرار بعد نجاح الفتح. كما أن `completeSecurityEvent` لا يحول الحدث الملغى إلى `SENT` لاحقًا، وهذه حماية مهمة من السباق.

### 4. حد للاستعادة

وجود `EVENT_LEASE_TIMEOUT_MILLIS` وحد أقصى لـ`MAX_EVENT_RECOVERY_ATTEMPTS` يمنع الحلقة اللانهائية.

## المشكلات المكتشفة

### P0 — الاستعادة لا تعني إعادة التنفيذ

`recoverStaleSecurityEvents()` يعيد الحدث إلى `PENDING`، لكنه لا يرسل Intent جديدًا ولا يضيفه إلى WorkManager ولا يملك طابورًا يستمر حتى التنفيذ.

يحدث ذلك في:

- `CameraForegroundService.onCreate()`.
- `SecurityViewModel.init()`.

في كلتا الحالتين تتم استعادة الحالة فقط. إذا لم يصل حدث جديد ولم يفتح المستخدم التطبيق أو ينفذ مسارًا آخر، فقد يبقى الحدث `PENDING` إلى أجل غير محدد.

**الأثر:** الحدث لا يضيع من التخزين، لكنه لا يُنفذ تلقائيًا.

### P0 — حدث حديث يتوقف ثم يصل Intent قبل انتهاء المهلة

عند إعادة إنشاء الخدمة، تستدعي `onCreate()` الاستعادة، لكن الاستعادة لا تعتبر الحدث حديثًا عالقًا حتى تمر دقيقتان. إذا وصل `onStartCommand` بعد إعادة إنشاء العملية مباشرة ومعه نفس `eventId`:

```text
status = IN_PROGRESS
claimSecurityEvent(eventId) = false
return
```

لا توجد جدولة لاحقة لهذا الحدث، فيبقى عالقًا حتى تنفذ جهة أخرى `recoverStaleSecurityEvents()` بعد انتهاء المهلة.

**الأثر:** احتمال فقدان التنفيذ بعد إعادة تشغيل سريعة أو إعادة إنشاء الخدمة.

### P1 — استعادة `IN_PROGRESS` لا تفرق بين الالتقاط والإرسال

الحالة الواحدة تغطي مراحل مختلفة:

```text
التقاط الصورة
تحديد الموقع
إنشاء MIME message
إرسال SMTP
حفظ SENT
```

إذا توقف التطبيق بعد نجاح SMTP وقبل حفظ `SENT`، فإن إعادة الحدث قد ترسل البريد مرة ثانية. لا يمكن منع هذه الحالة بشكل مضمون مع SMTP مباشر فقط.

**الحل:** إضافة حالات أدق مثل:

```text
CAPTURE_IN_PROGRESS
READY_TO_SEND
SEND_IN_PROGRESS
SENT
```

ومعرّف idempotency لدى خادم وسيط أو خدمة إرسال تدعم منع التكرار.

### P1 — `SharedPreferences.apply()` لا يضمن الكتابة قبل توقف العملية

تغييرات الحالات والعدادات تستخدم `apply()`. هذا مناسب عادةً، لكنه يكتب في الذاكرة أولًا ثم يطلب الكتابة غير المتزامنة. الإيقاف المفاجئ مباشرة بعد تغيير الحالة قد يجعل آخر حالة غير موجودة على القرص.

**الحل:**

- استخدام DataStore أو قاعدة بيانات بمعاملة.
- أو استخدام `commit()` فقط للانتقالات الحرجة جدًا، مع إدراك أنه يحجب الخيط ويجب ألا يُستعمل على Main Thread.

### P1 — الإقلاع لا يعيد معالجة الأحداث صراحةً

`BootReceiver` يستدعي `CountdownScheduler.rescheduleFromPrefs()`، ويبدأ خدمة المراقبة في بعض الإصدارات، لكنه لا يملك مسارًا واضحًا مثل:

```text
recover events -> enqueue unique recovery work -> process when allowed
```

وعلى Android 14+ يتجنب بدء خدمة الكاميرا بعد الإقلاع، وهذا صحيح من ناحية القيود، لكنه يعني أن الأحداث المستعادة لا تُرسل تلقائيًا من هناك.

### P1 — لا توجد جدولة فريدة للأحداث الأمنية

يوجد `CaptureRetryWorker` للمؤقت، لكنه لا يعالج طابور `SecurityEvent`. لذلك لا توجد آلية موثوقة لإعادة تشغيل حدث `PENDING` بعد:

- إعادة التشغيل.
- رفض بدء الخدمة.
- توقف التطبيق.
- فشل مؤقت في إنشاء الخدمة.

### P2 — `claimNextSecurityEvent()` غير مستخدمة

هناك مساران للمطالبة:

- `claimNextSecurityEvent()`.
- `claimSecurityEvent(id)`.

المسار الفعلي يستخدم الثاني فقط. وجود مسار غير مستخدم يزيد احتمالات اختلاف السلوك مستقبلًا، ويجب إما حذفه أو اعتماد موزع واحد رسمي.

## السيناريوهات الحرجة

| السيناريو | الحالة الحالية | الحكم |
|---|---|---|
| توقف قبل `claim` | يبقى `PENDING`، وقد يعاد عند Intent جديد | مقبول جزئيًا |
| توقف بعد `claim` مباشرة | يبقى `IN_PROGRESS` دقيقتين | يحتاج موزع استعادة |
| إعادة تشغيل بعد أكثر من دقيقتين | يتحول إلى `PENDING` عند فتح التطبيق/الخدمة | لا يضمن التنفيذ |
| إعادة تشغيل Android 14+ | BootReceiver لا يبدأ كاميرا خلفية | متوقع نظاميًا، يلزم إشعار/تفاعل مرئي |
| نجاح الفتح أثناء `IN_PROGRESS` | يتحول إلى `CANCELLED` | جيد |
| SMTP نجح ثم توقف قبل `SENT` | قد يعاد البريد | غير قابل للحسم مع SMTP مباشر |
| Intent مكرر أثناء `IN_PROGRESS` | يُرفض | جيد، لكن قد لا توجد إعادة جدولة |
| حدثان مختلفان متزامنان | يعاد الثاني محليًا بعد انتهاء الأول | جيد داخل نفس العملية فقط |

## التصميم التصحيحي المقترح

### 1. فصل الحجز عن الحالة

أضف إلى الحدث:

```text
leaseOwner
leaseStartedAt
nextAttemptAt
lastErrorCode
```

ولا تعتمد على `updatedAt` وحده لمعرفة سبب التوقف.

### 2. إنشاء موزع أحداث دائم

أنشئ مسارًا واحدًا فقط:

```text
recover -> find PENDING/expired IN_PROGRESS
      -> enqueue unique work per event
      -> worker claims event transactionally
      -> execute permitted visible flow
      -> persist result
```

كل حدث يملك اسم عمل فريدًا، مثل:

```text
security-event-{eventId}
```

واستخدم سياسة تمنع استبدال عمل قائم دون قصد.

### 3. منع إعادة التقاط خفية بعد الإقلاع

بعد إعادة التشغيل:

- استعد الحدث إلى `PENDING`.
- أظهر إشعارًا واضحًا بأن هناك تنبيهًا معلقًا.
- نفّذ التقاط الصورة فقط عندما يكون السياق مرئيًا ومسموحًا به.
- لا تبدأ كاميرا خفية من `BOOT_COMPLETED` أو من callback خلفي لمحاولة تجاوز Android.

### 4. تمييز الإرسال عن الالتقاط

لا تجعل استعادة حدث SMTP تعيد التقاط صورة جديدة تلقائيًا. خزّن نتيجة الصورة ومكانها، ثم أعد إرسال نفس الملف إن كان موجودًا. هذا يمنع:

- صورًا متعددة لنفس الحدث.
- اختلاف الموقع بين المحاولات.
- إرسال رسالة لا تطابق سجل الحدث.

### 5. حدود واضحة

- مهلة الحجز: دقيقتان أو قيمة قابلة للضبط.
- استعادة قصوى: محاولتان أو ثلاث.
- backoff: دقيقة، ثم خمس دقائق، ثم فشل نهائي.
- حذف الملفات المؤقتة عند فشل نهائي.
- الاحتفاظ بالسجل النهائي لأغراض التشخيص.

### 6. ضمانات الترتيب

إذا وجدت أحداث متعددة:

- عالج الأقدم أولًا.
- لا تعالج أكثر من حدث واحد داخل عملية الالتقاط.
- لا تسمح لنجاح فتح القفل بإلغاء حدث جديد وقع بعد النجاح؛ يلزم مقارنة `timestamp` أو `unlockSessionId`.

## الاختبارات المطلوبة

### اختبارات وحدة

1. `IN_PROGRESS` بعمر أقل من دقيقتين لا يستعاد.
2. `IN_PROGRESS` بعمر أكثر من دقيقتين يتحول إلى `PENDING`.
3. الاستعادة الثالثة تحول الحدث إلى `FAILED`.
4. حدث `CANCELLED` لا يتحول إلى `SENT`.
5. `claimSecurityEvent` الثاني يرجع `false`.
6. نجاح الفتح يلغي `PENDING` و`IN_PROGRESS`.
7. حدثان مختلفان لا يحصلان على نفس `eventId`.
8. الحقول القديمة التي لا تحتوي `updatedAt` تُقرأ باستخدام timestamp.

### اختبارات عملية

1. قتل العملية بعد claim مباشرة.
2. إعادة تشغيل الجهاز أثناء التقاط الصورة.
3. إيقاف الشبكة أثناء SMTP.
4. نجاح SMTP ثم قتل العملية قبل حفظ `SENT`.
5. فتح القفل الصحيح أثناء تنفيذ الحدث.
6. وصول Intent مكرر.
7. Android 14+ بعد الإقلاع.
8. فتح التطبيق بعد إعادة التشغيل ومراجعة الإشعار والسجل.

## الخلاصة

الجزء الحالي يمنع بعض التكرار ويحتوي على حد للاستعادة، لكنه لا يمثل **طابور استعادة مكتملًا**. أكبر فجوتين هما:

1. استعادة الحدث إلى `PENDING` دون موزع يضمن إعادة تشغيله.
2. رفض حدث `IN_PROGRESS` حديث بعد إعادة إنشاء الخدمة دون جدولة بديلة.

قبل اعتبار الميزة موثوقة، يجب إضافة موزع واحد دائم للأحداث، وفصل التقاط الصورة عن إرسال البريد، واعتماد مسار مرئي ومتوافق مع Android بدل محاولة تشغيل الكاميرا خفية بعد الإقلاع.
