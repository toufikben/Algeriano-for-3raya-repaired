# تقرير المراجعة النهائية للمشروع

## الحكم التنفيذي

المشروع في حالة جيدة من ناحية البناء الآلي، واختبارات الوحدة، وتنظيم التخزين، ومنع تكرار الأحداث، وحماية بيانات اعتماد البريد. آخر Commit برمجي هو `9f90344`، والفرع `main` مطابق للنسخة البعيدة، وآخر تشغيل GitHub Actions نجح في الاختبارات وLint وبناء Debug APK وRelease APK وRelease AAB والتحقق من artifacts.

لا توجد مشكلة برمجية مؤكدة تمنع البناء أو الاختبارات في المراجعة الحالية. ومع ذلك، لا يمكن إعلان خلو المشروع من جميع المخاطر دون اختبار جهاز Android حقيقي؛ لأن قيود تشغيل الكاميرا من الخلفية، وسلوك إعادة التشغيل، والصلاحيات، وقيود الشركات المصنعة لا يمكن إثباتها بالكامل عبر CI.

## نطاق المراجعة

شملت المراجعة حالة Git، سجل التغييرات الأخيرة، Manifest، إعدادات Gradle، Workflow الخاص بـ GitHub Actions، خدمة الكاميرا، WorkManager، BootReceiver، CountdownReceiver، Room، Migration من JSON، آلة حالات الأحداث، تشفير بيانات الاعتماد، قواعد النسخ الاحتياطي، EmailSender، إدارة WakeLock، إدارة HandlerThread، حذف الصور، وسجلات Logcat.

## نتائج التحقق

| المجال | النتيجة | الدليل أو الحد |
|---|---|---|
| حالة الفرع | ناجحة | `main` مطابق لـ `origin/main` ولا توجد تغييرات معلقة |
| فحص whitespace | ناجح | `git diff --check` |
| Unit tests | ناجحة | GitHub Actions Run `34047171735` |
| Lint | ناجح | GitHub Actions Run `34047171735` |
| Debug APK | ناجح | تم البناء والتحقق ورفع artifact |
| Release APK | ناجح | تم البناء؛ غير موقع لغياب أسرار التوقيع |
| Release AAB | ناجح | تم البناء والتحقق من محتوى archive |
| Room | ناجح | كيانات وDAO وMigration وبناء CI ناجح |
| حماية كلمة المرور | جيدة | AES-GCM داخل Android Keystore مع معالجة إبطال المفتاح |
| النسخ الاحتياطي | مقيد | SharedPreferences وقاعدة Room وصور التطبيق مستبعدة، بما في ذلك ملفات WAL وSHM |
| اختبار جهاز حقيقي | غير متوفر | يحتاج Android 14 أو أحدث وجهازًا فعليًا |

## الإصلاحات التي أضيفت أثناء المراجعة

تم جعل استبدال الأحداث والسجلات داخل Room عملية Transaction واحدة بدل حذف البيانات ثم إدخالها في عمليتين منفصلتين. يمنع ذلك فقد البيانات الجزئي إذا توقف التطبيق بين العمليتين.

تم نقل حذف الصور والسجلات إلى `Dispatchers.IO` بدل تنفيذه على خيط واجهة المستخدم. يقلل ذلك احتمال تجمد الواجهة عند حذف عدد كبير من الصور.

تم استبعاد ملفات قاعدة Room الجانبية `security_data.db-wal` و`security_data.db-shm` من قواعد النسخ الاحتياطي ونقل البيانات، إضافة إلى استبعاد ملف قاعدة البيانات الأساسي.

تم منع عرض رسائل استثناء SMTP الخام للمستخدم أو داخل سجل الحدث. يعرض التطبيق رسالة عامة، بينما يسجل نوع الاستثناء فقط دون محتواه.

## نقاط القوة الحالية

يستخدم التطبيق `eventId` لضمان عدم إنشاء سجل متطفل مكرر عند إعادة إرسال الحدث نفسه. كما يفصل بين نتيجة الصورة ونتيجة الموقع ونتيجة البريد.

تستخدم آلة الحالات انتقالات صريحة، ولا تسمح بإحياء الأحداث النهائية مثل `SENT` و`FAILED_FINAL` و`CANCELLED`.

تستخدم بيانات اعتماد البريد تشفير AES-GCM مع مفتاح Android Keystore. لا يتم تخزين كلمة المرور بصيغة نصية في ملف SharedPreferences الجديد، وتوجد معالجة لبيانات الاعتماد القديمة.

لا يبدأ التطبيق خدمة كاميرا مخفية بعد الإقلاع على Android 14 أو أحدث. عند الحاجة، يعرض إشعارًا يطلب من المستخدم فتح التطبيق لاستئناف الحماية.

تتم إدارة WakeLock داخل `finally`، وتوجد معالجة لإغلاق الكاميرا وImageReader وHandlerThread.

## المخاطر والقيود المتبقية

### اختبار الجهاز الحقيقي — أولوية P0

لم يتم إثبات عمل الكاميرا والموقع والخدمة بعد إعادة التشغيل على جهاز Android فعلي. يجب اختبار Android 14 أو أحدث، مع وبدون صلاحيات الإشعارات، ومع تعطيل البطارية، وبعد إعادة تشغيل الجهاز، ومع انقطاع الشبكة.

### أسرار توقيع Release — أولوية P0 للنشر

CI ينجح، لكن أسرار التوقيع غير مهيأة. لذلك فإن Release APK الحالي غير موقع. يجب إضافة الأسرار إلى GitHub Actions Secrets قبل استخدام AAB في النشر.

### ملاحظات KSP في CI

ظهرت Annotation داخل CI من KSP مرتبطة بعدم توفر `ApplicationManager` في سياق IntelliJ، لكنها لم تفشل المهمة، ولم تمنع الاختبارات أو البناء. يفضل تحديث KSP أو AGP لاحقًا إذا استمرت الملاحظة، لكن لا يوجد حاليًا فشل وظيفي مرتبط بها.

### واجهة المستخدم تحتفظ بكلمة المرور في الذاكرة

تحتاج واجهة Compose إلى قيمة كلمة المرور لعرض حقل الإدخال، لذلك توجد نسخة نصية في حالة `SecurityUiState` أثناء استخدام الشاشة. هذا ليس تخزينًا دائمًا، لكنه يظل مخاطرة نظرية في الذاكرة. يمكن تحسينه لاحقًا بتقليل مدة الاحتفاظ، ومسح الحقل بعد الحفظ، وعدم إعادة تعبئته تلقائيًا من التخزين.

### تخزين الصور محليًا

الصور تحفظ داخل مساحة ملفات التطبيق وتستبعد من النسخ الاحتياطي. ما زال ينبغي اختبار حذف الصور فعليًا، ومعالجة الصور orphaned التي لم يعد لها سجل، ووضع سياسة احتفاظ زمنية حتى لا يتراكم التخزين.

## التوصيات التالية

ينبغي تنفيذ اختبار الجهاز الحقيقي أولًا قبل إضافة وظائف جديدة. بعد ذلك ينبغي إضافة Job دوري لتنظيف الصور القديمة غير المرتبطة بسجلات أو التي تجاوزت مدة الاحتفاظ، مع اختبار فشل الحذف.

ينبغي تهيئة أسرار التوقيع وإنتاج AAB موقع، ثم التحقق من التثبيت فوق إصدار سابق.

ينبغي تحسين ملاحظات KSP في CI إذا استمرت في الظهور، ويفضل تثبيت نسخ متوافقة صراحة من Kotlin وKSP وRoom وAGP.

ينبغي إضافة اختبارات Migration فعلية تبدأ ببيانات JSON قديمة وتتحقق من عدد الأحداث والسجلات والحقول الحساسة بعد النقل إلى Room.

## الخلاصة

المشروع اجتاز المراجعة البرمجية والآلية الحالية دون عطل مؤكد يمنع البناء أو الاختبارات. تم إغلاق العيوب التي ظهرت أثناء المراجعة الأخيرة ورفعها في Commit مستقل. لا يزال اعتماد المشروع للإنتاج مشروطًا باختبار جهاز حقيقي وإعداد توقيع Release؛ وهذان شرطان تشغيليان لا يمكن استبدالهما بنجاح CI وحده.
