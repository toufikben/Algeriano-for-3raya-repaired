# تقرير تدقيق شامل للكود

**المستودع:** `toufikben/phone-fortress`
**الفرع المفحوص:** `main`
**الإصدار الظاهر في السجل:** `Prepare Play Store release 1.1.0`
**نطاق الفحص:** قراءة بنية المشروع والكود المتعلق بمدير الجهاز، محاولات فتح القفل، الخدمة الأمامية، الكاميرا، الموقع، البريد، المؤقت، التخزين، الإقلاع، الصلاحيات، وسياسة الخصوصية، مع بدء بناء Gradle والاختبارات.

## 1. الحكم المختصر

المشروع منظم مبدئيًا ويحتوي على فصل معقول بين الواجهة و`ViewModel` والتخزين والخدمة، كما أن استخدام `AtomicBoolean` و`WakeLock` والمهلات الزمنية ومحاولات إعادة التشغيل يدل على محاولة معالجة مشاكل التشغيل الخلفي. لكن الوظيفة الأساسية ليست مضمونة منطقيًا أو تشغيليًا:

1. الكود **لا يرسل لكل واحدة من المحاولات الثلاث**؛ يبدأ الإرسال عند المحاولة الثالثة، ثم يرسل لكل محاولة لاحقة إلى أن يحدث فتح ناجح.
2. تشغيل خدمة كاميرا أمامية من `DeviceAdminReceiver.onPasswordFailed` في الخلفية يتعارض غالبًا مع قيود Android الحديثة، خصوصًا Android 12–14+، وقد يفشل حتى مع منح الصلاحيات.
3. التقاط صورة وموقع دون تفاعل ظاهر بعد فشل فتح الجهاز وظيفة شديدة الحساسية. لا أوصي بجعلها صامتة أو خفية، ولا أقدّم تعديلات تزيد قدرتها على المراقبة السرية.
4. كلمة مرور Gmail App Password محفوظة كنص صريح في `SharedPreferences`، وهذه مخاطرة أمنية كبيرة.
5. مسار إعادة المحاولة لا يعيد المحاولة عند فشل التقاط الصورة أو فشل البريد؛ يعيدها تقريبًا فقط عند فشل بدء الخدمة أو وجود التقاط سابق قيد التنفيذ.
6. لا توجد اختبارات فعلية للمنطق الحرج؛ الاختبارات الحالية تبدو اختبارات قالب/واجهة، ولا تختبر العتبة أو السباق أو فشل SMTP أو قيود الخدمة.

## 2. التتبع الفعلي لمنطق المحاولات

المسار الحالي هو:

```text
Android Device Admin
  -> onPasswordFailed()
  -> registerFailedUnlockAttempt()
  -> إذا failedAttempts < 3: لا صورة ولا بريد
  -> إذا failedAttempts >= 3: تشغيل CameraForegroundService
  -> captureImageSilently()
  -> fetchCurrentLocation()
  -> EmailSender.sendSecurityAlert()
  -> addLog()
  -> إشعار محلي
```

وعند `onPasswordSucceeded()` يتم تصفير العداد وإلغاء المؤقت.

### هل يرسل عند كل مرة من الثلاث؟

**لا.** وفقًا للكود في `MyDeviceAdminReceiver.kt:24–33`، العدّاد يزداد، لكن استدعاء الخدمة لا يحدث إلا عند `failedAttempts >= threshold`، والقيمة الافتراضية للعتبة 3. النتيجة:

| الفشل | السلوك الحالي |
|---:|---|
| 1 | تسجيل داخلي فقط، بلا صورة وبلا بريد |
| 2 | تسجيل داخلي فقط، بلا صورة وبلا بريد |
| 3 | محاولة التقاط صورة/موقع وإرسال بريد |
| 4 وما بعده | محاولة التقاط/إرسال في كل فشل لاحق حتى نجاح فتح القفل |
| نجاح فتح القفل | تصفير العداد وإلغاء المؤقت |

إذا كان المقصود «تنبيه واحد بعد ثلاث محاولات»، فالمنطق قريب من ذلك، مع ملاحظات التوافق أدناه. إذا كان المقصود «ثلاث رسائل، رسالة لكل فشل»، فالكود لا يحقق ذلك.

## 3. نتائج عالية الخطورة

### 3.1 تشغيل الكاميرا من الخلفية غير موثوق

`MyDeviceAdminReceiver.kt:31–40` يطلب تشغيل `CameraForegroundService` مباشرة من callback خاص بفشل كلمة مرور الجهاز. وفي `CameraForegroundService.kt:112–161` تتم ترقية الخدمة إلى foreground بنوع الكاميرا/الموقع.

المشكلة ليست مجرد إذن `CAMERA`: إصدارات Android الحديثة تفرض قيودًا على بدء foreground service من الخلفية وعلى خدمات الكاميرا والموقع ذات الصلاحيات while-in-use. لذلك قد يحدث أحد الآتي:

- `ForegroundServiceStartNotAllowedException` عند بدء الخدمة.
- `SecurityException` عند `startForeground` أو عند محاولة استخدام الكاميرا.
- بدء الخدمة ثم فشل الاستخدام بسبب عدم وجود سياق مرئي أو قيود النظام.
- اختلاف السلوك بين الشركات المصنعة وإصدارات Android.

وجود `try/catch` يمنع انهيار التطبيق لكنه لا يجعل الوظيفة تعمل؛ هو فقط يسجل الفشل.

**التوصية الآمنة:** لا تعتمد على التقاط خفي في الخلفية. اجعل الحدث يولد إشعارًا واضحًا يطلب من مالك الجهاز فتح التطبيق أو الضغط على إجراء صريح، ثم نفّذ الالتقاط فقط في سياق مرئي ومعلن وبعد موافقة واضحة.

### 3.2 تناقض بين اسم “المراقبة” والسلوك الفعلي

`ACTION_START_MONITORING` في `CameraForegroundService.kt:101–107` لا يبدأ مراقبة مستقلة؛ الفرع ينفذ `Unit` فقط. المراقبة الفعلية تعتمد على callback من Device Admin. هذا ليس خطأ بحد ذاته، لكنه يسبب التباسًا في التصميم والواجهة والاختبارات: الخدمة تبدو كأنها تراقب باستمرار، بينما هي لا تستقبل أحداث فتح القفل بنفسها.

### 3.3 مشكلة إعادة المحاولة

في `CameraForegroundService.kt:302–304` يتم استدعاء `clearCountdown()` بعد انتهاء المعالجة، حتى لو كان إرسال البريد فاشلًا (`emailSuccess == false`) أو لم تُلتقط صورة. لذلك لا توجد إعادة محاولة حقيقية لفشل SMTP أو GPS أو الكاميرا.

وفي `CaptureRetryWorker.kt:20–32` تعاد المحاولة عند فشل **بدء الخدمة** فقط. أما إذا بدأت الخدمة ثم:

- فشلت الكاميرا؛
- انتهت مهلة الكاميرا؛
- لم يتوفر الموقع؛
- فشل Gmail SMTP؛

فغالبًا تنتهي العملية بنجاح ظاهري للعامل أو تُمسح حالة المؤقت دون استكمال موثوق.

**التوصية الآمنة:** افصل حالة الالتقاط عن حالة الإرسال، وسجل حالات صريحة مثل `CAPTURE_FAILED`, `LOCATION_UNAVAILABLE`, `SEND_RETRYABLE`, `SEND_SENT`. لا تعتبر العملية ناجحة إلا بعد النتيجة المقصودة، مع حد أقصى واضح وإشعار للمستخدم.

### 3.4 كلمة مرور البريد مخزنة كنص صريح

في `SecurityPrefs.kt:71–77` تحفظ `email` و`password` مباشرة في `SharedPreferences`. استخدام App Password أفضل من كلمة مرور Gmail الرئيسية، لكنه لا يلغي الخطر: استخراج بيانات التطبيق أو نسخة احتياطية أو جهاز مكسور الحماية قد يكشفها.

**التوصية:** استخدام Android Keystore مع تخزين مشفر مثل `EncryptedSharedPreferences` أو آلية تشفير مكافئة، وعدم تسجيل قيمة السر أو تضمينها في رسائل الخطأ. يجب أيضًا توفير حذف فوري للبيانات السرية عند تعطيل الميزة.

### 3.5 الصور والموقع بيانات شخصية شديدة الحساسية

الصور تحفظ في `getExternalFilesDir(null)/intruder_photos`. هذا أفضل من مجلد عام، لكنه لا يجعل البيانات غير حساسة. كما أن `android:allowBackup="true"` مع قواعد نسخ احتياطي غير محددة يستحق المراجعة حتى لا تنتقل السجلات أو الصور أو بيانات التهيئة إلى نسخة احتياطية غير متوقعة.

سياسة الخصوصية الحالية تذكر الصورة والموقع وSMTP، وهذه نقطة جيدة، لكنها لا تعالج بما يكفي:

- أن الالتقاط قد يحدث من حدث خلفي؛
- مدة الاحتفاظ الدقيقة؛
- تشفير الأسرار محليًا؛
- ما يحدث عند فشل الإرسال؛
- نطاق النسخ الاحتياطي؛
- متطلبات موافقة المستخدمين الآخرين الموجودين أمام الكاميرا.

## 4. مشكلات منطقية وتشغيلية متوسطة

### 4.1 العداد ليس ذريًا بالكامل

`registerFailedUnlockAttempt()` معلّمة `@Synchronized`، لكن القراءة والكتابة عبر `SharedPreferences.apply()` غير متزامنة مع التخزين الدائم. غالبًا يكفي ذلك في الاستخدام العادي، لكنه ليس ضمانًا قويًا ضد فقدان آخر قيمة عند الإيقاف المفاجئ. كما أن callback يأتي من النظام وليس من تدفق اختبار مضبوط.

### 4.2 استمرارية الخدمة

`isServicePersistent()` يعيد قيمة `prefs.isTrackingEnabled`. عند تفعيل الحماية تبقى الخدمة foreground بعد كل عملية التقاط. هذا قد يكون مقصودًا لإبقاء الحماية نشطة، لكنه يزيد استهلاك البطارية ويجعل التطبيق يبدو كأنه يستخدم الكاميرا باستمرار حتى عندما لا يلتقط فعليًا. يجب أن تكون صياغة الإشعار دقيقة، وأن يكون للمستخدم تحكم واضح.

### 4.3 إشعارات متفائلة

في `showAlertNotification()` تعرض الرسالة «تم التقاط الصورة» عندما تكون `emailSent == false`، حتى لو كانت الصورة `null`؛ الكود في `processIntruderCapture()` لا يميز نص الإشعار بين:

- صورة التقطت فعلًا؛
- فشل التقاط الصورة؛
- موقع غير متاح؛
- فشل الإرسال.

هذا قد يعطي المستخدم انطباعًا غير صحيح.

### 4.4 فشل البريد لا يمنع اعتبار الحدث مكتملًا

يتم إنشاء سجل دائم بعد فشل الإرسال، وهذا جيد للتشخيص، لكن لا يوجد طابور إرسال موثوق أو إعادة محاولة مرتبطة بالسجل. `emailSent` يصبح `false` ولا يوجد مسار لاحق لإعادة الإرسال.

### 4.5 التعامل مع الموقع

`getCurrentLocation()` يستخدم `PRIORITY_HIGH_ACCURACY` ثم fallback إلى آخر موقع معروف. هذا منطقي تقنيًا، لكنه يعني أن «الموقع المرسل» قد يكون قديمًا، بينما نص البريد لا يوضح عمر الموقع أو دقته. ينبغي حفظ `accuracy` ووقت الموقع ووسم صريح مثل «موقع حالي» أو «آخر موقع معروف».

### 4.6 دورة حياة الكاميرا والمهلة

يوجد إغلاق من عدة callbacks (`onImageAvailable`, `onDisconnected`, `onError`, timeout). معظم المسارات محمية بمحاولات، لكن الإغلاق المتزامن قد يؤدي إلى سباقات أو نتائج متأخرة من `ImageReader`. كما أن `acquireLatestImage()` قد يسقط صورًا وسيطة؛ وهذا ليس مشكلة كبيرة لالتقاط واحد، لكنه يحتاج اختبارًا على أجهزة فعلية.

### 4.7 استخدام `printStackTrace`

يوجد `printStackTrace()` في `EmailSender.kt` و`SecurityPrefs.kt` ومواضع تنظيف الكاميرا. ينبغي استخدام logging مضبوط لا يكشف تفاصيل تشغيلية أو معلومات قد تكون حساسة في سجلات الجهاز.

## 5. تعارضات وازدواجيات في المشروع

- `play-services-location` مضاف مرة فعالة ومرة معلقة في `app/build.gradle.kts`؛ التعليق لا يسبب تعارضًا، لكنه يدل على تنظيف غير مكتمل.
- توجد اعتمادات كثيرة معلقة أو غير مستخدمة ظاهريًا: CameraX وFirestore وFirebase Auth وDataStore وغيرها. هذا لا يكسر البناء عادة، لكنه يزيد تعقيد الصيانة وحجم المشروع ومخاطر التحديث.
- الخدمة معلنة `camera|location` حتى أثناء `ACTION_START_MONITORING` الذي لا يستخدم الكاميرا فعليًا في ذلك الوقت. الأفضل تصميم دورة حياة وصلاحيات تتوافق مع الاستخدام الحقيقي، لا إعلان قدرة أوسع من اللازم.
- `BootReceiver` يتجاهل بدء خدمة الكاميرا على Android 14+، بينما الواجهة قد تعرض أن التتبع مفعّل. ينتج عن ذلك حالة «مفعّل في التفضيلات، غير عامل بعد إعادة التشغيل» حتى يفتح المستخدم التطبيق ويعيد التشغيل يدويًا.
- `attemptsCount` في الواجهة يستخدم `uiState.logs.size`، بينما التخزين يحتفظ أيضًا بـ`totalAttempts`. بعد الوصول إلى 50 سجلًا سيظهر العدد ثابتًا عند 50 بدل العدد الحقيقي.

## 6. الصلاحيات والخصوصية

الصلاحيات المطلوبة واسعة: الكاميرا، الموقع الدقيق، الإشعارات، foreground service، الإقلاع، تعطيل تحسين البطارية، ومدير الجهاز. بعضها مبرر لوظيفة معلنة، لكن الجمع بين مدير الجهاز والكاميرا والموقع والبريد يتطلب شرحًا شديد الوضوح وموافقة صريحة.

نقطة إيجابية: الواجهة تعرض حالة الصلاحيات، وتوجد سياسة خصوصية عربية تذكر الصورة والموقع وGmail SMTP. لكن يجب عدم وصف الوظيفة بأنها حماية موثوقة على كل الأجهزة، ويجب منع أي سلوك يمكن تفسيره كمراقبة سرية.

## 7. الاختبارات والبناء

توجد ملفات اختبار قالبية في `app/src/test` و`app/src/androidTest`، ولا يظهر ضمن الفحص اختبار يغطي:

- 1/2/3/4 محاولات فاشلة؛
- تصفير العداد بعد النجاح؛
- بدء الخدمة من callback خلفي؛
- فشل الصلاحيات؛
- فشل الكاميرا والمهلة؛
- الموقع الحالي مقابل آخر موقع؛
- SMTP timeout وفشل المصادقة؛
- عدم تكرار الإرسال عند duplicate intents؛
- إعادة التشغيل والإقلاع؛
- حذف الصور عند حذف السجلات.

تم تشغيل أمر البناء والاختبارات:

```text
./gradlew :app:assembleDebug :app:testDebugUnitTest --no-daemon
```

بدأ Gradle تنزيل التوزيعة وحل الاعتمادات أثناء جلسة الفحص. يلزم اعتماد النتيجة النهائية من مخرجات المهمة/جلسة البناء قبل اعتبار التجميع ناجحًا؛ لا ينبغي اعتبار بدء Gradle دليلًا على نجاح البناء.

## 8. ترتيب الأولويات المقترح

### أولوية P0 — قبل أي نشر

1. إيقاف أي التقاط أو إرسال خفي، وتحويل السلوك إلى إشعار واضح وتفاعل مرئي وموافقة صريحة.
2. توضيح متطلبات الدعم: عدم وعد المستخدم بأن الكاميرا ستعمل من الخلفية على Android 12–14+.
3. تشفير App Password باستخدام Keystore وعدم حفظها plain text.
4. مراجعة النسخ الاحتياطي وحذف الأسرار والصور من أي نطاق backup غير مقصود.
5. تحديث سياسة الخصوصية لتطابق التنفيذ الفعلي ومدة الاحتفاظ وحالات الفشل.

### أولوية P1 — صحة المنطق

1. تحديد المطلوب بدقة: تنبيه بعد 3 محاولات أم تنبيه لكل محاولة.
2. إصلاح آلة الحالات للالتقاط والإرسال؛ لا تمسح المؤقت عند فشل الإرسال.
3. إضافة طابور إرسال محدود وإعادة محاولة idempotent، أو حذف فكرة إعادة المحاولة إن لم تكن مطلوبة.
4. تصحيح نصوص الإشعارات بحيث تعكس الصورة والموقع والبريد كلًّا على حدة.
5. حفظ دقة الموقع وعمره، وعدم تسمية آخر موقع معروف بأنه موقع حالي.

### أولوية P2 — الجودة والصيانة

1. إضافة اختبارات وحدة لـ`SecurityPrefs` وعتبة المحاولات.
2. إضافة اختبارات تكامل/محاكاة لمسارات الخدمة، دون استخدام كاميرا حقيقية في الاختبارات.
3. إزالة الاعتمادات والتعليقات غير المستخدمة.
4. استبدال `printStackTrace` بـlogger مضبوط.
5. فصل `CaptureResult` و`SendResult` عن تخزين السجل لتسهيل الاختبار.
6. استخدام العدد الحقيقي للمحاولات في الواجهة بدل `logs.size`.

## 9. استنتاج نهائي حول سؤال «هل يعمل جيدًا وهل هو منطقي؟»

**منطقي جزئيًا كتصميم تنبيه أمني معلن، لكنه ليس موثوقًا كتنفيذ حالي.** العتبة تعمل حسابيًا على مستوى `DeviceAdminReceiver`، لكن الالتقاط والإرسال من الخلفية غير مضمونين على Android الحديث، ولا يتم الإرسال لكل من المحاولات الثلاث، وإعادة المحاولة لا تغطي معظم حالات الفشل، والسر محفوظ بطريقة غير آمنة.

أقرب صياغة صادقة للسلوك الحالي هي:

> «بعد تفعيل حماية معلنة وصلاحيات مناسبة، يحاول التطبيق عند بلوغ ثلاث محاولات فتح فاشلة متتالية تشغيل خدمة أمامية لالتقاط صورة والحصول على موقع وإرسال بريد. قد يمنع نظام Android الحديث بدء هذه الخدمة من الخلفية، وقد تفشل الكاميرا أو الموقع أو SMTP؛ وفي هذه الحالات يسجل التطبيق الفشل محليًا بدرجات متفاوتة.»

لا أوصي بنشر هذه الميزة بصورتها الحالية، ولا أوصي بتعديلها لتصبح أكثر خفاءً. الإصلاح الصحيح هو جعلها شفافة، مرئية، محدودة، مشفرة، ومختبرة على إصدارات Android والأجهزة المستهدفة.


## ملحق — نتيجة إعادة البناء بعد تثبيت JDK

تم تثبيت OpenJDK 21 الكامل بنجاح، وأصبح `javac 21.0.12` متاحًا. عند إعادة تشغيل البناء، تجاوز المشروع مشكلة Java compiler، لكنه فشل قبل تجميع كود التطبيق بسبب عدم وجود Android SDK أو عدم تعريف مساره:

```text
SDK location not found.
Define a valid SDK location with an ANDROID_HOME environment variable
or by setting the sdk.dir path in local.properties.
```

ويطلب المشروع حاليًا `compileSdk = 36` مع `minorApiLevel = 1` و`targetSdk = 36`. لذلك لا يمكن اعتبار البناء ناجحًا أو فاشلًا بسبب Kotlin/Android code حتى يتم تثبيت منصة Android SDK المطلوبة وتعريف `ANDROID_HOME` أو `local.properties`. فشل البناء الثاني **بيئي** أيضًا، لكنه مختلف عن فشل JDK الأول.

أوامر التحقق المطلوبة في بيئة Android/CI:

```bash
export JAVA_HOME=/usr/lib/jvm/java-21-openjdk-amd64
export ANDROID_HOME=/path/to/android-sdk
export PATH="$JAVA_HOME/bin:$ANDROID_HOME/platform-tools:$ANDROID_HOME/cmdline-tools/latest/bin:$PATH"
# ثم تثبيت platform المطلوبة، مثل android-36، إذا كانت مطلوبة في البيئة
./gradlew :app:assembleDebug :app:testDebugUnitTest --no-daemon
```
