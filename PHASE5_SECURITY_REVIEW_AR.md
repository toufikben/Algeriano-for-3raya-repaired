# تقرير المرحلة الخامسة: مراجعة أمان Android

## نطاق المراجعة

تمت مراجعة المستودع `toufikben/phone-fortress` على الفرع `main` قبل التعديل. المراحل السابقة الخاصة بسلامة الأحداث، وفصل نتائج الالتقاط والموقع والبريد، ومنع تكرار `IntruderLog`، وإصلاح توقيع CI، ومراجعة `BootReceiver` كانت موجودة في سجل GitHub، وكان آخر تشغيل ناجح قد بنى APK وAAB.

## النتائج السابقة التي تم التحقق منها

| المجال | النتيجة |
|---|---|
| تكرار الأحداث | يوجد ربط بالسجل عبر `eventId` واختبار لإعادة المحاولة |
| حالات الالتقاط/الموقع/البريد | مفصولة في الخدمة والسجل |
| `BootReceiver` | `android:exported="false"` |
| نوع الخدمة | `camera|location` مع صلاحيات foreground service المتخصصة |
| CI | آخر تشغيل متاح نجح في بناء Debug APK وRelease APK وRelease AAB والتحقق من artifacts |
| حالة العمل قبل التعديل | الفرع المحلي مطابق لـ `origin/main` ولا توجد تغييرات معلقة |

## الفجوة التي عولجت

كان `BootReceiver` يمنع بدء `CameraForegroundService` مباشرة على Android 14+، لكنه كان قبل ذلك يستدعي `SecurityEventDistributor.enqueuePending(context)` قبل شرط المنع. هذا يسمح بإنشاء `WorkManager` يعمل لاحقًا ويحاول تشغيل خدمة كاميرا foreground من الخلفية، وهو مسار غير آمن وغير متوافق مع قيود Android الحديثة.

تمت المعالجة عبر:

1. عدم جدولة الأحداث المعلقة من مسار الإقلاع عندما تكون الحماية مفعلة على Android 14 أو أحدث.
2. إضافة حارس داخل `SecurityEventWorker` يعيد `Result.success()` دون تحويل الحدث إلى فشل أو إعادة محاولة لا نهائية؛ تبقى البيانات المعلقة محفوظة حتى يبدأ المستخدم مسارًا مرئيًا.
3. إضافة إشعار واضح قابل للنقر يطلب من المستخدم فتح التطبيق لاستئناف الحماية، بدل تنفيذ التقاط كاميرا خفي بعد الإقلاع.
4. الإبقاء على جدولة الأحداث في الإصدارات الأقدم، وعلى مسار المستخدم المرئي داخل `SecurityViewModel`.
5. إضافة نصوص عربية توضّح سبب تعليق المعالجة بعد إعادة التشغيل.

## قرار الأمان والتوافق

لا يعتمد التطبيق بعد هذا الإصلاح على تشغيل الكاميرا خفية من `BOOT_COMPLETED` في Android 14+. وجود `ForegroundService` لا يلغي قيود بدء خدمة الكاميرا من الخلفية؛ لذلك أصبح السلوك معلنًا للمستخدم ومربوطًا بفتح التطبيق، مع الاحتفاظ بالأحداث بدل فقدها.

## التحقق المنفذ

تم تنفيذ فحص حالة Git، ومراجعة Manifest، ونسخ SDK، والصلاحيات، ومسارات بدء الخدمة، وتشغيلات GitHub Actions السابقة، ثم فحص `git diff --check` بعد التعديل. لا يتوفر Android SDK محليًا في بيئة العمل، لذلك لا يمكن إعلان نجاح Gradle المحلي من هذه البيئة. سيتم الاعتماد على GitHub Actions للتحقق من التجميع والاختبارات بعد رفع التغيير.

## الاختبارات المطلوبة بعد الدمج

ينبغي تنفيذ اختبار جهاز حقيقي على Android 14 أو أحدث للتحقق من أن إعادة التشغيل تعرض الإشعار، ولا تبدأ الكاميرا تلقائيًا، وأن فتح التطبيق واستئناف الحماية يعالج الأحداث المعلقة، مع التأكد من عمل الصلاحيات والإشعارات وعدم تكرار السجلات.
