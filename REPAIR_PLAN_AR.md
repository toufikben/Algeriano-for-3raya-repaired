# خطة إصلاح وتحسين شاملة

## 1. السلوك المطلوب وتحديده بدقة

السلوك المطلوب هو:

```text
فشل 1: تسجيل المحاولة فقط
فشل 2: تسجيل المحاولة فقط
فشل 3: إنشاء تنبيه والتقاط/إرسال وفق مسار آمن ومعلن
فشل 4 وما بعده: إنشاء تنبيه والتقاط/إرسال لكل فشل لاحق
نجاح فتح القفل: إيقاف التنبيهات المتتابعة وتصفير العداد
```

هذا يختلف عن إرسال ثلاث رسائل فقط. من ناحية العداد، الكود الحالي قريب من هذا السلوك لأن الشرط هو `failedAttempts >= threshold`، لكن التنفيذ يفشل أحيانًا بسبب قيود Android الخلفية، وحالة الخدمة، وتعارض الطلبات، وفشل الكاميرا أو البريد.

> لا أوصي بتنفيذ التقاط صورة أو موقع بشكل خفي أو دون إشعار ظاهر. الإصلاح الآمن هو أن يكون جمع البيانات معلنًا، وأن يعرف مالك الجهاز ما الذي سيحدث ومتى، وأن يستخدم الالتقاط المرئي/المصرح به أو إشعارًا يتطلب إجراءً واضحًا عندما يمنع Android التشغيل من الخلفية.

## 2. أولويات الإصلاح

| الأولوية | المجال | النتيجة المطلوبة |
|---|---|---|
| P0 | الخصوصية والسلامة | إيقاف أي سلوك خفي، وإظهار إشعار واضح، ومراجعة الموافقة وسياسة الخصوصية |
| P0 | توافق Android | عدم الاعتماد على بدء كاميرا من callback خلفي كأنه مضمون |
| P0 | الأسرار | تشفير App Password وعدم تخزينها كنص صريح |
| P1 | منطق المحاولات | آلة حالات واضحة تمنع فقدان الأحداث والطلبات المكررة |
| P1 | الإرسال | فصل الالتقاط عن البريد، وإعادة المحاولة فقط للأخطاء القابلة للإعادة |
| P1 | الاختبارات | اختبار العتبة، النجاح، التكرار، التزامن، وفشل الصلاحيات |
| P2 | الجودة | تنظيف الاعتمادات، السجلات، النسخ الاحتياطي، وتحسين واجهة الحالة |

## 3. المرحلة صفر: تجهيز بيئة البناء

قبل الحكم على أخطاء الكود:

1. تثبيت JDK 21 الكامل، وليس JRE فقط.
2. تثبيت Android SDK Platform وBuild Tools المتوافقة مع `compileSdk 36`.
3. تعريف `ANDROID_HOME` أو `sdk.dir` في `local.properties` محليًا فقط، وعدم رفع `local.properties` إلى Git.
4. تشغيل:

```bash
./gradlew clean
./gradlew :app:assembleDebug :app:testDebugUnitTest --no-daemon --stacktrace
```

5. إضافة فحص CI يطبع Java وGradle وAndroid SDK، ثم يفشل برسالة واضحة عند غياب أي منها.

## 4. المرحلة الأولى: إعادة تصميم منطق المحاولات

### 4.1 آلة حالات مقترحة

بدل توزيع المنطق بين `DeviceAdminReceiver` و`Service` و`Worker`، يُعرّف مسار واضح:

```text
FAILED_UNLOCK_RECEIVED
  -> increment consecutive counter atomically
  -> if count < 3: persist event and stop
  -> if count >= 3: create alert event with unique id
  -> hand off to approved/visible capture flow
  -> capture result
  -> location result
  -> send result
  -> persist final state
```

الحالات المقترحة:

- `RECORDED`: سُجل الفشل فقط.
- `ALERT_PENDING`: بلغ الحد ويحتاج تنفيذ التنبيه.
- `CAPTURE_PENDING`: طلب التقاط معلن ومصرح به.
- `CAPTURED`: اكتملت الصورة.
- `LOCATION_READY` أو `LOCATION_UNAVAILABLE`.
- `SEND_PENDING`.
- `SENT`.
- `FAILED_RETRYABLE`.
- `FAILED_FINAL`.
- `CANCELLED_AFTER_SUCCESS`: حدث فتح صحيح وأُوقفت التنبيهات اللاحقة.

### 4.2 منع التكرار والسباقات

كل حدث يحتاج `eventId` فريدًا، ويُحفظ قبل بدء العمل. عند وصول Intent مكرر:

- لا تنشئ تنبيهًا ثانيًا لنفس `eventId`.
- لا تبدأ كاميرا ثانية إذا كان هناك التقاط جارٍ.
- لا تستبدل WorkManager عملًا قائمًا عشوائيًا باستخدام `REPLACE` إذا كان ذلك قد يلغي محاولة مهمة؛ استخدم سياسة تناسب الغرض، مثل `KEEP` أو طابورًا لكل حدث.

يجب أن يكون التسجيل الذري للعداد والحدث واضحًا. الأفضل نقل الحالة من `SharedPreferences` إلى DataStore أو قاعدة بيانات صغيرة إذا أصبحت الأحداث متعددة ومتزامنة.

### 4.3 تصفير العداد

عند `onPasswordSucceeded`:

1. سجّل حدث النجاح.
2. زِد رقم جيل/نسخة الحالة `unlockSessionId` أو استخدم وقت النجاح.
3. صفّر العداد.
4. ألغِ التنبيهات غير المنفذة التي تخص الجيل السابق.
5. لا تحذف سجلًا أُرسل بالفعل.

بهذا لا يؤدي حدث فشل قديم متأخر إلى إرسال تنبيه بعد نجاح فتح القفل.

## 5. المرحلة الثانية: معالجة فشل التنفيذ

### 5.1 سبب الفشل الأساسي

المشكلة الجوهرية ليست فقط في `try/catch`. Android الحديث قد يمنع بدء أو استخدام خدمة كاميرا من الخلفية، خصوصًا عندما يأتي الطلب من `DeviceAdminReceiver` أو من `AlarmManager` أو `WorkManager` دون سياق مرئي.

لذلك توجد مسارات آمنة:

- **المسار الموصى به:** يسجل callback محاولة الفشل ويعرض إشعارًا واضحًا. يفتح مالك الجهاز التطبيق أو يضغط إجراءً صريحًا لبدء الالتقاط المرئي.
- **المسار البديل:** إذا كان الهدف تنبيهًا أمنيًا فقط، أرسل إشعارًا نصيًا/صوتيًا أو سجّل الحدث، ولا تحاول تجاوز قيود Android لفتح الكاميرا خفية.
- لا ينبغي جعل الالتقاط الصامت من الخلفية شرطًا لصحة المنتج؛ لأنه لن يكون موثوقًا عبر الأجهزة وقد يتعارض مع متطلبات الخصوصية والمتجر.

### 5.2 إذا فشلت الكاميرا

- سجّل `CAPTURE_FAILED` مع سبب مختصر.
- لا تعرض «تم التقاط الصورة» إذا كانت الصورة `null`.
- لا تعتبر العملية ناجحة لمجرد أن الخدمة بدأت.
- اعرض للمستخدم: «تعذر الالتقاط؛ تم تسجيل المحاولة».
- لا تعيد المحاولة بلا حدود.

### 5.3 إذا فشل الموقع

- فرّق بين «الموقع الحالي غير متاح» و«آخر موقع معروف».
- خزّن الدقة ووقت أخذ الموقع.
- لا ترسل رابطًا وتصفه بأنه موقع حالي إذا كان من `lastLocation`.
- اجعل الموقع اختياريًا في نتيجة التنبيه بدل إسقاط التنبيه كله.

### 5.4 إذا فشل البريد

- اعتبر فشل SMTP قابلًا للإعادة فقط في حالات الشبكة والمهلة المؤقتة.
- اعتبر فشل المصادقة أو بيانات الاعتماد خطأ نهائيًا حتى يصلحه المستخدم.
- استخدم WorkManager مع backoff محدود، ومفتاح فريد لكل حدث.
- لا تمسح حالة المؤقت أو الحدث قبل نجاح الإرسال أو الوصول إلى حد المحاولات.
- لا تحفظ كلمة المرور في بيانات WorkManager أو السجلات.

## 6. المرحلة الثالثة: إصلاح الأمان

### 6.1 App Password

الوضع الحالي يخزن البريد وكلمة المرور في `SharedPreferences` كنص واضح. يجب:

1. استخدام Android Keystore.
2. تشفير App Password قبل التخزين.
3. منع ظهور السر في Logcat أو رسائل الاستثناء.
4. مسح السر عند اختيار المستخدم تعطيل إعداد البريد.
5. إضافة زر «حذف بيانات الاعتماد» منفصل عن حذف السجلات.
6. إخفاء كلمة المرور في الواجهة افتراضيًا.

### 6.2 الصور والسجلات

- استخدام التخزين الداخلي أو مجلد app-specific مع تحديد مدة احتفاظ.
- حذف الصور المرتبطة عند حذف السجلات.
- وضع حد للحجم والعدد.
- منع إدراج الصور والأسرار في النسخ الاحتياطي غير المقصود.
- تشفير الصور محليًا إذا كان المنتج يحتفظ بها بعد التنبيه.

### 6.3 مدير الجهاز

- شرح سبب تفعيل Device Admin بلغة دقيقة.
- توفير مسار تعطيل كامل يوقف الخدمة ويلغي المؤقت ويمسح/يحافظ على البيانات حسب اختيار المستخدم.
- تحديث سياسة الخصوصية لتذكر أن بيانات الموقع والصورة قد تكون حساسة وأن بعض الأحداث تحدث بعد فشل فتح القفل.

## 7. المرحلة الرابعة: إصلاح دورة حياة الخدمة

1. لا تجعل `ACTION_START_MONITORING` يوحي بأن الكاميرا تعمل دائمًا؛ اجعل اسم الحالة يوضح أنها «حماية مفعّلة» فقط.
2. لا تُعلن نوع كاميرا/موقع أوسع من الاستخدام الفعلي عندما لا توجد عملية التقاط.
3. راجع `startForegroundService` في كل مسار خلفي مع قيود Android المستهدف.
4. اجعل الإشعار foreground واضحًا وقابلًا لإيقاف الحماية منه.
5. استخدم `stopForeground` و`stopSelf` في مسارات النجاح والفشل بدقة.
6. لا تعتمد على أن بدء الخدمة يعني نجاح الالتقاط.
7. اجعل إغلاق `CameraDevice` و`ImageReader` idempotent، واختبر timeout وcallbacks المتأخرة.

## 8. المرحلة الخامسة: إصلاح المؤقت وWorker

الوضع الحالي يزيد `countdownRetryCount` ثم يستخدم `ExistingWorkPolicy.REPLACE`. هذا قد يلغي طلبًا سابقًا ويجعل العدّاد لا يطابق العمل الحقيقي.

التعديل المقترح:

- تعريف `countdownEventId`.
- إنشاء عمل فريد لذلك الحدث.
- استخدام `KEEP` لمنع استبدال محاولة قائمة دون قصد.
- إعادة المحاولة فقط عند خطأ قابل للإعادة.
- زيادة العداد بعد جدولة العمل الفعلية، لا قبلها فقط.
- تسجيل نتيجة Worker النهائية.
- عدم مسح `countdownCapturePending` إلا بعد نتيجة موثوقة أو قرار نهائي ظاهر للمستخدم.

## 9. المرحلة السادسة: تحسينات واجهة وحالة المستخدم

يجب أن تعرض الواجهة بوضوح:

- عدد المحاولات المتتالية الحالي.
- هل الحماية مفعّلة أم أن النظام منع تشغيل التنفيذ.
- هل آخر حدث سجل فقط، أم التقط صورة، أم أرسل بريدًا.
- وقت ومصدر الموقع: حالي/آخر معروف.
- سبب الفشل بطريقة مفهومة.
- زر إيقاف الحماية وإلغاء المؤقت.
- عدد المحاولات الحقيقي، لا `logs.size` فقط؛ لأن السجل يحتفظ بحد أقصى 50 عنصرًا.

## 10. اختبارات مطلوبة

### 10.1 اختبارات منطقية

| السيناريو | النتيجة المتوقعة |
|---|---|
| فشل أول | لا تنبيه التقاط، عداد = 1 |
| فشل ثانٍ | لا تنبيه التقاط، عداد = 2 |
| فشل ثالث | حدث تنبيه واحد، عداد = 3 |
| فشل رابع | حدث تنبيه جديد |
| نجاح بعد الثالث | تصفير العداد وإلغاء الأحداث غير المنفذة |
| Intent مكرر | لا إرسال مزدوج لنفس الحدث |
| فشل SMTP مؤقت | يبقى `SEND_PENDING` ويعاد وفق حد محدود |
| فشل مصادقة SMTP | `FAILED_FINAL` مع طلب تصحيح الإعداد |
| عدم وجود كاميرا | سجل فشل واضح دون ادعاء نجاح |
| عدم وجود موقع | يستمر التنبيه مع وسم الموقع غير متاح |
| إيقاف الحماية | إلغاء الخدمة والمؤقت والأحداث الجديدة |

### 10.2 اختبارات أندرويد فعلية

يجب اختبار Android 10 و11 و12 و13 و14 و15/الإصدار المستهدف، وعلى جهازين أو ثلاثة من مصنعين مختلفين، مع:

- الشاشة مقفلة.
- التطبيق في الخلفية.
- وضع توفير البطارية.
- عدم منح الكاميرا أو الموقع.
- عدم وجود شبكة.
- إعادة التشغيل.
- تفعيل Device Admin ثم تعطيله.

لا يكفي اختبار المحاكي لإثبات أن الكاميرا ستعمل من callback خلفي.

## 11. خطة تنفيذ عملية على دفعات

### الدفعة A — السلامة والبيئة

- تجهيز Android SDK وCI.
- تشفير بيانات الاعتماد.
- ضبط النسخ الاحتياطي.
- تحديث سياسة الخصوصية والإشعارات.
- عدم تغيير سلوك الالتقاط ليصبح أكثر خفاءً.

### الدفعة B — منطق الأحداث

- إضافة نموذج حدث و`eventId`.
- إصلاح العتبة والتصفير ومنع التكرار.
- فصل التسجيل عن الالتقاط والإرسال.
- إصلاح العدد الظاهر في الواجهة.

### الدفعة C — الفشل وإعادة المحاولة

- بناء نتائج صريحة للكاميرا والموقع والبريد.
- إضافة backoff محدود للبريد فقط.
- إصلاح Worker وعدم استخدام `REPLACE` بطريقة تفقد أحداثًا.
- تحديث الإشعارات لتطابق النتيجة الحقيقية.

### الدفعة D — الاختبارات

- اختبارات وحدة للعداد والحالات.
- اختبارات تكامل للـWorker والخدمة باستخدام fakes.
- اختبارات جهاز فعلية لمسارات Android الخلفية.
- فحص Logcat وعدم تسريب الأسرار.

### الدفعة E — مراجعة النشر

- بناء Debug وRelease.
- فحص Manifest والصلاحيات.
- مراجعة سياسة الخصوصية.
- اختبار إلغاء Device Admin.
- اختبار إزالة التطبيق وحذف البيانات.
- عدم النشر قبل إثبات أن وصف المتجر يطابق السلوك المرئي الفعلي.

## 12. الخلاصة

الشرط الحسابي الذي تريده واضح وقابل للتنفيذ: يبدأ التنبيه من الفشل الثالث ويستمر في الفشل الرابع وما بعده حتى نجاح الفتح. المشكلة الأساسية ليست في شرط `>= 3`، بل في الاعتماد على تشغيل كاميرا من الخلفية في Android الحديث، إضافة إلى غياب آلة حالات موثوقة، وإعادة محاولة غير مكتملة، وتخزين سر غير آمن.

الإصلاح الصحيح هو جعل كل حدث قابلًا للتتبع، ومنع التكرار، وتسجيل النتيجة الحقيقية، واستخدام إشعار واضح وتفاعل معلن عند الحاجة إلى الكاميرا، مع عدم محاولة تجاوز قيود النظام أو تحويل الوظيفة إلى مراقبة خفية.
