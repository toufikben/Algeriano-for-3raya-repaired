# تقرير الفحص المحلي للكود

## نطاق الفحص

تم فحص النسخة المحلية عند commit `c8b74f5` دون تثبيت حزم أو تشغيل تعليمات من ملفات المشروع أو رفع تغييرات جديدة. شمل الفحص حالة Git، مراجع حالات الأحداث، مسار المؤقت، WorkManager، الخدمة الأمامية، السجلات، وإعدادات البناء.

## النتيجة العامة

المستودع نظيف، والنسخة المحلية مطابقة لآخر commit مرفوع إلى المستودع الخاص. لم تظهر ملفات أسرار معروفة مثل `.env` أو `local.properties` أو ملفات keystore في الفحص المحدود.

لم يمكن تشغيل Gradle لأن Android SDK غير معرف:

```text
ANDROID_HOME=
ANDROID_SDK_ROOT=
no local.properties
```

لذلك لا يمكن اعتبار الكود مجمعًا أو مجتازًا للاختبارات runtime قبل توفير SDK.

## الملاحظات ذات الأولوية

### P1 — تكرار سجل نفس الحدث أثناء إعادة إرسال البريد

في `CameraForegroundService` يتم إنشاء `IntruderLog` وإضافته في كل تنفيذ لمسار الحدث. عند فشل SMTP القابل لإعادة المحاولة، يعاد تشغيل الخدمة لنفس `eventId`، وبالتالي يمكن أن تظهر عدة سجلات لنفس الحدث وتزداد `totalAttempts` عدة مرات.

**الأثر:** سجل الواجهة قد يوحي بوجود محاولات اختراق متعددة، بينما هي محاولات إرسال متعددة لحدث واحد.

**الإصلاح المقترح:** جعل السجل idempotent باستخدام `eventId` كمعرف السجل، أو إضافة `securityEventId` إلى `IntruderLog` وتحديث السجل السابق بدل إضافة سجل جديد عند إعادة الإرسال.

### P1 — لا يوجد backoff زمني حقيقي للبريد

بعد فشل SMTP القابل لإعادة المحاولة، يعاد إدخال الحدث إلى `SecurityEventDistributor` مباشرة. يوجد حد أقصى للمحاولات، لكن لا توجد فترات انتظار متزايدة مثل 1 دقيقة ثم 5 دقائق ثم 15 دقيقة.

**الأثر:** عند انقطاع الشبكة قد تحدث محاولات متقاربة، مما يستهلك البطارية ويزيد احتمالات الحظر المؤقت من خادم البريد.

**الإصلاح المقترح:** حفظ `nextAttemptAt` في الحدث واستخدام `setInitialDelay` في WorkManager وفق backoff محدود، أو استخدام `BackoffPolicy.EXPONENTIAL` مع حد أقصى واضح.

### P1 — نجاح Worker لا يعني نجاح تشغيل الخدمة

`SecurityEventWorker` يرجع `Result.success()` فور نجاح استدعاء `startForegroundService`. هذا لا يثبت أن الخدمة نجحت في `startForeground` أو أن الكاميرا فتحت أو أن البريد أرسل.

**الأثر:** الفشل اللاحق لا يظهر للعامل نفسه، ويعتمد النظام على lease recovery لاحقًا.

**الإصلاح المقترح:** إبقاء النتيجة النهائية في سجل الحدث، وإضافة عامل مراقبة/استعادة يعتمد على حالة الحدث، مع عدم اعتبار بدء Intent نتيجة نهائية.

### P1 — التقاط غير مكتمل قد يتحول إلى إرسال نصي ناجح

`recordCaptureResult` يضع الحدث في `SEND_PENDING` حتى إذا كانت الصورة `null` أو الموقع غير متاح. بعد ذلك قد يرسل SMTP رسالة بلا صورة أو موقع، وتصبح الحالة `SENT` لأن البريد نجح.

**الأثر:** `SENT` تعني نجاح البريد فقط، وليس نجاح الصورة والموقع، وقد لا يكون ذلك واضحًا بما يكفي.

**الإصلاح المقترح:** فصل النتيجة إلى حقول مستقلة مثل `photoCaptured`, `locationAvailable`, `emailSent`، واستخدام حالة نهائية مركبة أو رسالة واضحة في السجل.

### P2 — بقايا `printStackTrace`

توجد استدعاءات `e.printStackTrace()` في `CameraForegroundService` داخل إيقاف خيط الخلفية وإغلاق الكاميرا.

**الإصلاح المقترح:** استبدالها بـ`Log.e(TAG, ...)` دون تسجيل الأسرار أو محتوى الاعتماد.

### P2 — رسائل استثناء SMTP قد تصل إلى واجهة المستخدم

يتم استخدام `localizedMessage` لفشل البريد، وقد يحتوي على نص خادم SMTP أو معلومات تقنية لا يحتاجها المستخدم.

**الإصلاح المقترح:** عرض رسالة مصنفة للمستخدم، مع حفظ رمز تقني محدود في Logcat فقط، مثل `SMTP_TIMEOUT` أو `SMTP_AUTH_FAILED`.

### P2 — الاعتماد على `SharedPreferences.apply()` للانتقالات الحرجة

حالات الأحداث والعدادات تحفظ باستخدام `apply()`. هذا غير متزامن وقد يفقد آخر انتقال عند إيقاف العملية مباشرة.

**الإصلاح المقترح:** نقل طابور الأحداث إلى Room/DataStore، أو استخدام كتابة متزامنة مدروسة للانتقالات الحرجة فقط دون حجب Main Thread.

### P2 — حالة `CAPTURED` معرفة لكنها غير مستخدمة فعليًا

النموذج يعرف `CAPTURED`، لكن المسار ينتقل مباشرة من `IN_PROGRESS` إلى `SEND_PENDING` بعد حفظ نتيجة الالتقاط.

**الإصلاح المقترح:** إما إزالة الحالة غير المستخدمة أو اعتمادها كحالة فعلية قصيرة مع انتقال ذري إلى `SEND_PENDING`.

## نقاط سليمة في النسخة الحالية

- `eventId` فريد لكل حدث.
- استخدام `KEEP` للأعمال الفريدة يمنع تكرار WorkManager لنفس الحدث.
- إعادة المحاولة لا تعيد التقاط الصورة إذا كانت نتيجة الالتقاط محفوظة.
- أخطاء مصادقة SMTP مصنفة كغير قابلة لإعادة المحاولة.
- `FAILED_FINAL` لا يدخل قائمة الأحداث المعلقة.
- نجاح فتح القفل يلغي الأحداث غير المنتهية.
- زر إعادة ضبط المؤقت يعيد استخدام المدة المحفوظة ويعيد جدولة الإنذارات.
- `git diff --check` نجح.

## ترتيب الإصلاح المقترح

1. جعل `IntruderLog` idempotent حسب `eventId`.
2. إضافة `nextAttemptAt` وbackoff للبريد.
3. فصل حالة الصورة والموقع عن حالة البريد في السجل.
4. إزالة `printStackTrace` وتصنيف رسائل SMTP.
5. تشغيل Gradle بعد توفير Android SDK.
6. اختبار الجهاز الفعلي وإعادة التشغيل وفشل الخدمة.
7. نقل طابور الأحداث من JSON داخل SharedPreferences إلى تخزين معاملات أقوى.
